<Window x:Class="WpfIocDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfIocDemo"
        mc:Ignorable="d"
        Title="WPF IoC Demo Program" Height="600" Width="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="🚀 WPF IoC (Dependency Injection) Demo"
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>
        
        <!-- Description -->
        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,15">
            <Run Text="This program demonstrates how to use dependency injection (IoC) in WPF applications:"/>
            <LineBreak/>
            <Run Text="• OrderService injects IOrderRepository and IMessageSender via constructor injection"/>
            <LineBreak/>
            <Run Text="• MainViewModel injects OrderService"/>
            <LineBreak/>
            <Run Text="• All dependencies are configured in App.xaml.cs, achieving loose coupling design"/>
        </TextBlock>
        
        <!-- Control Panel -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="Order ID:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Text="{Binding OrderIdInput}" Width="100" VerticalAlignment="Center" Margin="0,0,10,0"/>
            
            <Button Content="📦 Create Order" 
                    Command="{Binding PlaceOrderCommand}"
                    Padding="10,5" Margin="0,0,10,0"/>
            
            <Button Content="📊 View Order Summary" 
                    Command="{Binding ShowOrdersCommand}"
                    Padding="10,5" Margin="0,0,10,0"/>
            
            <Button Content="🗑️ Clear Output" 
                    Command="{Binding ClearOutputCommand}"
                    Padding="10,5"/>
        </StackPanel>
        
        <!-- Output Area -->
        <Border Grid.Row="3" BorderBrush="Gray" BorderThickness="1" 
                Background="#F5F5F5" CornerRadius="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="10">
                <TextBlock Text="{Binding Output}" 
                           TextWrapping="Wrap" 
                           FontFamily="Consolas"
                           FontSize="12"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
